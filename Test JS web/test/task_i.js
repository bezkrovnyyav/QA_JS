/*
const assert = require("assert")


describe('webdriver.io page', () => {
    it('Open the browser & click the button', () => {
        browser.url('https://webdriver.io');
        const apiButton = $('[href="/docs/api.html"]');
        apiButton.click();
    });
	
	 it('Display url', () => {
        let browserUrl = 'https://webdriver.io/docs/api.html';
		console.log(browserUrl);
		});
	
	it('Display the title', () => {
		browser.url('https://webdriver.io/docs/api.html');

		const title = $('.postHeaderTitle');
		console.log("Title is: " + title.getText());
		});
		
	it('Display the Attribute', () => {
		const ref = $('//*[text()="JSONWire protocol"]');
        let attr = ref.getAttribute('href');
		browser.pause(2000);
        console.log("The attributeof element is: " + attr);
		});
		
	it('Input the text in the search field', () => {
        
        let input = $('#search_input_react');
        input.setValue('test is ');
        browser.pause(5000);
        input.addValue('DONE');
        })
	
})
	


*/